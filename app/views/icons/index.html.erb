<%= will_paginate %>

<ul class="icons">
  <% @icons.each do |icon| %>
		<i><%= image_tag(image_url(icon), width: "32", height: "32" ) %></i>
	<% end %>
</ul>

<%= will_paginate %>

<script>
	$(".paginate a").each(function(i,link){
		var hr = link.href;
		link.href = '#';
		link.attr('target',hr);
	});
</script>
